---
# Stack
name: Stack1
environment: DEV

stack:

#- node: database
#  depends_on:
#  attributes:
#    server_root_password: passw0rd
#    server_debian_password: passw0rd
#  run_list:
#  - recipe[mysql:server]
#  knife:
#    plugin: vagrant
#    box: ubuntu/trusty64
#    # Give VM and IP in a private network
#    subnet: 192.168.50.0/24

- node: wordpress
  depends_on:
#  - database
  attributes:
    wordpress:
      db:
        name:
        host:
        user:
        pass:
  run_list:
  - recipe[mysql:client]
  - recipe[wordpress]
  knife:
    plugin: vagrant
    box: ubuntu/trusty64
    # Give VM and IP in a private network
    subnet: 192.168.50.0/24
