---
# Stack
name: Stack1
environment: DEV

stack:

- node: database
  depends_on:
  run_list:
  host:
    name: host1
    domain: $DOMAIN
    ip:
    knife:
      plugin:
      args:

- node: wordpress
  depends_on:
  run_list:
  host:
    name: host1
    domain: $DOMAIN
    ip:
    knife:
      plugin:
      args:
